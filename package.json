{
  "name": "perch-gallery",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "generate-index": "node scripts/generate-index.mjs",
    "build": "node scripts/generate-index.mjs && astro build && node scripts/copy-catalog.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.5.0",
    "js-yaml": "^4.1.0"
  }
}
