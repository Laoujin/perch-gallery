{
  "name": "perch-gallery",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "validate": "node scripts/validate-catalog.mjs",
    "sync-stars": "node scripts/sync-github-stars.mjs",
    "build": "node scripts/validate-catalog.mjs && astro build && node scripts/copy-catalog.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.5.0",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "ajv": "8",
    "ajv-formats": "^3.0.1"
  }
}
