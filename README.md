Perch Gallery
=============

Community catalog for [Perch](https://github.com/laoujin/perch) — a cross-platform Windows-first
dotfiles and application settings manager.

Served via GitHub Pages at `https://laoujin.github.io/perch-gallery/`.

## Structure

```
catalog/
  index.yaml              # Master index of all entries
  apps/<id>.yaml           # Application definitions
  fonts/<id>.yaml          # Font definitions
  tweaks/<id>.yaml         # Windows registry tweak definitions
```
