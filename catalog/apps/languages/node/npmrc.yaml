type: app
name: .npmrc
category: Languages/Node
tags: [config, npm, node, dotfile]
description: npm configuration file
profiles: [developer]
os: [windows, linux, macos]
config:
  links:
    - source: .npmrc
      target:
        windows: "%USERPROFILE%/.npmrc"
        linux: "$HOME/.npmrc"
        macos: "$HOME/.npmrc"
