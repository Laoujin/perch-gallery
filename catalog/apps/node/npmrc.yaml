type: app
name: .npmrc
kind: dotfile
category: Development/Node
tags: [node, npm, config]
description: npm registry and behavior configuration
profiles: [developer]
os: [windows, linux, macos]
license: MIT
config:
  links:
    - source: .npmrc
      target:
        windows: "%USERPROFILE%/.npmrc"
        linux: "$HOME/.npmrc"
        macos: "$HOME/.npmrc"
